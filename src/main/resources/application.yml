spring:
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: "jdbc:mysql://localhost:3306/nht_transactional"
    password: "root"
    username: "root"
  batch:
    initialize-schema: always
    chunk-size: 10
    job:
      enabled: false